type UserProfile {
  firstName: String
  lastName: String
  birthDate: String
}

type UserSystem {
  isEmailApproved: Boolean!
  verificationToken: String
}

type User {
  _id: ID!
  email: String!
  password: String!
  createdAt: String!
  updatedAt: String!
  system: UserSystem!
  profile: UserProfile
}

input UserProfileInput {
  firstName: String
  lastName: String
  birthDate: String
}
